<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.zaidMansuri.quizapp.MainActivity"
    tools:openDrawer="start">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#1E0542"
            app:navigationIcon="@drawable/question_answer"
            app:title="Quiz"
            app:titleTextColor="@color/white" />


        <FrameLayout
            android:id="@+id/frame"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_above="@+id/bottomNav"
            android:layout_weight="1" />


        <me.ibrahimsn.lib.SmoothBottomBar
            android:id="@+id/bottomBar"
            android:layout_width="match_parent"
            android:layout_height="70dp"
            app:backgroundColor="#040130"
            app:duration="1000"
            app:iconSize="30dp"
            app:indicatorColor="#A7DC19"
            app:indicatorRadius="12dp"
            app:menu="@menu/bottom_menu"
            app:sideMargins="10dp"
            app:textColor="@color/white"
            app:textSize="15sp"
            app:iconTintActive="@color/darkblue"
            app:cornerRadius="25dp"
          />
    </LinearLayout>


    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="#000C53"
        app:headerLayout="@layout/navlayout"
        app:itemIconTint="#FFB300"
        app:itemTextColor="@color/white"
        app:menu="@menu/navmenu" />


</androidx.drawerlayout.widget.DrawerLayout>